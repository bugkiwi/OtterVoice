{
  "name": "@ottervoice/provider-utils",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared building blocks for OtterVoice providers: token broker, HTTP error mapping, SSE parsing.",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "development": "./src/index.ts",
      "react-native": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc -p tsconfig.json",
    "test": "bun test"
  },
  "peerDependencies": {
    "@ottervoice/core": "workspace:^"
  },
  "devDependencies": {
    "@ottervoice/core": "workspace:^"
  },
  "typedoc": {
    "entryPoints": [
      "./src/index.ts"
    ]
  }
}
