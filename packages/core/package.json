{
  "name": "@ottervoice/core",
  "version": "0.2.0-alpha.0",
  "type": "module",
  "description": "Platform-agnostic core for OtterVoice: VoiceSession, state machine, provider router, events, and mock providers.",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "development": "./src/index.ts",
      "react-native": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "rm -rf dist tsconfig.tsbuildinfo && tsc -p tsconfig.json",
    "test": "bun test"
  },
  "typedoc": {
    "entryPoints": [
      "./src/index.ts"
    ]
  }
}
