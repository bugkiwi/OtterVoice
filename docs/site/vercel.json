{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd ../.. && bun install --frozen-lockfile && cd examples/web && bun install --frozen-lockfile",
  "buildCommand": "cd ../.. && bun run build && cd docs/site && bun run build",
  "outputDirectory": "dist",
  "regions": ["sin1"],
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 60,
      "supportsCancellation": true
    }
  }
}
