---
title: API reference
description: Parameter and type docs generated from source JSDoc via TypeDoc.
---

These pages are **generated by TypeDoc** from public exports and JSDoc. Narrative package guides live under [Packages](/docs/en/packages/); treat the generated tables as the source of truth for signatures.

## Suggested order

1. [`@ottervoice/core`](/docs/en/reference/api/ottervoice-core/) — `VoiceSession`, `VoiceSessionConfig`, events, provider interfaces
2. Matching runtime — `runtime-web` / `runtime-react-native` / `runtime-node`
3. Chosen `@ottervoice/provider-*` and `@ottervoice/provider-utils`

If a parameter is undocumented, add JSDoc in source and run `bun run build:api` (see root [`AGENTS.md`](https://github.com/bugkiwi/OtterVoice/blob/main/AGENTS.md)).
