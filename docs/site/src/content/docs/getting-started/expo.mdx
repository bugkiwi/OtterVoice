---
title: Expo / React Native 接入
description: 通过注入 Expo 音频能力驱动 runtime-react-native。
---

对应示例：[`examples/react-native-expo`](https://github.com/bugkiwi/OtterVoice/tree/main/examples/react-native-expo)

完整绑定见示例中的 [`expo-adapters.ts`](https://github.com/bugkiwi/OtterVoice/blob/main/examples/react-native-expo/src/expo-adapters.ts)。

```bash
cd examples/react-native-expo
cp .env.example .env   # 只配置你控制的策略网关 URL
bun run start
```

## 结构

推荐输入：**16 kHz · mono · PCM16**。Runtime 不硬依赖 Expo 包，由你注入小型接口：

```ts
import { createExpoRuntime } from '@ottervoice/runtime-react-native';
import { createVoiceSession } from '@ottervoice/core';

const runtime = createExpoRuntime({
  input: { /* Expo AudioStream / 录音绑定 */ },
  output: { /* AudioPlaylist / 播放绑定 */ },
});

const session = createVoiceSession({
  mode: 'full_duplex',
  pipeline: 'audio_llm', // 示例默认走 Audio LLM
  asrPartial: false, // 标准模式只做一次终句 ASR，避免客户端放大请求数。
  runtime,
  providers: { asr, llm, audioLlm },
});
```

采集分两路：

- `delivery: 'stream'` — 小 PCM 缓冲 → 流式 ASR
- `delivery: 'turn'` — 完整 WAV → batch ASR / Audio LLM

标准模式使用默认的 `audioLlmStartTiming: 'after_asr_final'`，先等待权威终句
字幕，再产生一次付费回复请求。`after_audio` 与滚动 ASR 是明确的低延迟/高请求
模式；只有服务端已按用户、会话和 profile 设置预算与速率限制时才开启。

助手播放期间可暂停编码采集，但保留音量回调以继续 barge-in。细节见 [runtime-react-native](/docs/packages/runtime-react-native/)。
